var funcCreeps = require('func.creeps');

var jobReserver = {
  run: function(creep,debug){
    if (debug == 2) creep.say("🏁");

    funcCreeps.workAtPost(creep, debug);
  }
}

module.exports = jobReserver;
